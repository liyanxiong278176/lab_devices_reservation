<?xml version="1.0" encoding="UTF-8"?>
<!--
  Maven settings for Docker builds only.
  China network: Maven Central (repo.maven.apache.org) times out from inside the
  Docker BuildKit VM, so route all traffic through the Aliyun mirror which is
  reliably reachable. The host build is unaffected (this file is only COPYed in
  by the backend Dockerfile).
-->
<settings xmlns="http://maven.apache.org/SETTINGS/1.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.2.0 https://maven.apache.org/xsd/settings-1.2.0.xsd">
    <mirrors>
        <mirror>
            <id>aliyun-public</id>
            <name>Aliyun Public Mirror</name>
            <url>https://maven.aliyun.com/repository/public</url>
            <mirrorOf>*</mirrorOf>
        </mirror>
    </mirrors>
</settings>
