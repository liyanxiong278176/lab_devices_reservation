# 留空 → useAiWebSocket / useWebSocket 走 window.location 同源 /api/ws
# (vite dev server proxy 到 :8080,配 ws:true 透传 upgrade)。直连 :8080 在浏览器
# 跨源 ws upgrade 会被 CORS 拒,且 http-only proxy 不能 upgrade,造成 SockJS
# 探测所有 transport 都失败但 stompjs 仍报 CONNECTED(老版本 SockJS 已知问题)。
VITE_WS_BASE=
